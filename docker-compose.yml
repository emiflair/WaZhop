services:
  # MongoDB Database
  mongodb:
    image: mongo:7.0
    container_name: wazhop-mongodb
    restart: unless-stopped
    environment:
      MONGO_INITDB_DATABASE: wazhop
    # ports:
    #   - "27017:27017"  # Commented out to avoid conflict with local MongoDB
    volumes:
      - mongodb_data:/data/db
    networks:
      - wazhop-network

  # Backend API
  backend:
    build:
      context: ./server
      dockerfile: Dockerfile
    container_name: wazhop-backend
    restart: unless-stopped
    ports:
      - "5001:5001"
    environment:
      - NODE_ENV=production
      - PORT=5001
      - MONGODB_URI=mongodb://mongodb:27017/wazhop
      - JWT_SECRET=${JWT_SECRET:-dev-jwt-secret-key-change-in-production}
      - JWT_EXPIRE=30d
      - APP_BASE_URL=${APP_BASE_URL:-http://localhost:3000}
      - EMAIL_PROVIDER=${EMAIL_PROVIDER:-brevo}
      - BREVO_API_KEY=${BREVO_API_KEY}
      - BREVO_SENDER_EMAIL=${BREVO_SENDER_EMAIL}
      - BREVO_SENDER_NAME=${BREVO_SENDER_NAME:-WaZhop}
      - SMS_PROVIDER=${SMS_PROVIDER:-brevo}
      - BREVO_SMS_SENDER=${BREVO_SMS_SENDER:-WaZhop}
      - CLOUDINARY_CLOUD_NAME=${CLOUDINARY_CLOUD_NAME}
      - CLOUDINARY_API_KEY=${CLOUDINARY_API_KEY}
      - CLOUDINARY_API_SECRET=${CLOUDINARY_API_SECRET}
      - ADMIN_EMAIL=${ADMIN_EMAIL:-admin@wazhop.com}
      - ADMIN_PASSWORD=${ADMIN_PASSWORD:-Admin123!}
    depends_on:
      - mongodb
    networks:
      - wazhop-network

  # Frontend
  frontend:
    build:
      context: ./client
      dockerfile: Dockerfile
      args:
        - VITE_API_URL=/api
    container_name: wazhop-frontend
    restart: unless-stopped
    ports:
      - "3000:80"
    depends_on:
      - backend
    networks:
      - wazhop-network

networks:
  wazhop-network:
    driver: bridge

volumes:
  mongodb_data:
    driver: local
