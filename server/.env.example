# ===========================================
# WaZhop Server Environment Variables
# ===========================================

# --- Core ---
NODE_ENV=development
PORT=5000
MONGODB_URI=mongodb://localhost:27017/wazhop
APP_BASE_URL=http://localhost:5173

# --- Security ---
# Generate with: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=your-super-secret-jwt-key-at-least-64-characters-long-recommended
JWT_EXPIRE=30d

# Encryption Key (for sensitive data like payment keys)
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
ENCRYPTION_KEY=your-encryption-key-at-least-32-characters-long

# --- Logging ---
LOG_LEVEL=info

# --- Frontend URL (legacy CORS) ---
CLIENT_URL=http://localhost:5173

# --- Brevo Email/SMS ---
EMAIL_PROVIDER=brevo
BREVO_API_KEY=your-brevo-api-key
BREVO_SENDER_EMAIL=support@wazhop.ng
BREVO_SENDER_NAME=WaZhop

# For Brevo SMS (optional; may require enabling transactional SMS)
SMS_PROVIDER=brevo
BREVO_SMS_SENDER=WaZhop

# --- Cloudinary Configuration ---
CLOUDINARY_CLOUD_NAME=your-cloudinary-cloud-name
CLOUDINARY_API_KEY=your-cloudinary-api-key
CLOUDINARY_API_SECRET=your-cloudinary-api-secret

# --- Admin Configuration ---
ADMIN_EMAIL=admin@wazhop.ng
ADMIN_PASSWORD=ChangeThisToAStrongPassword123!

# ===========================================
# OPTIONAL CONFIGURATIONS
# ===========================================

# --- Rate Limiting (Optional - defaults in code) ---
# RATE_LIMIT_WINDOW_MS=900000
# RATE_LIMIT_MAX_REQUESTS=100

# --- Payment Integration (Required for Subscriptions) ---
FLUTTERWAVE_PUBLIC_KEY=your-flutterwave-public-key
FLUTTERWAVE_SECRET_KEY=your-flutterwave-secret-key
# PAYSTACK_PUBLIC_KEY=
# PAYSTACK_SECRET_KEY=

# --- Monitoring (Optional) ---
# SENTRY_DSN=
# DATADOG_API_KEY=

# --- Redis (Optional - for advanced rate limiting) ---
# REDIS_URL=redis://localhost:6379

# --- WhatsApp Business API (Optional - Premium Feature) ---
WHATSAPP_API_URL=https://graph.facebook.com/v18.0
WHATSAPP_PHONE_NUMBER_ID=your-phone-number-id-here
WHATSAPP_ACCESS_TOKEN=your-whatsapp-access-token-here
WHATSAPP_BUSINESS_ACCOUNT_ID=your-business-account-id-here
WHATSAPP_WEBHOOK_SECRET=your-webhook-secret-here
WHATSAPP_WEBHOOK_VERIFY_TOKEN=your-verify-token-here

# ===========================================
# SECURITY NOTES
# ===========================================
# 1. NEVER commit .env file with real values
# 2. Use strong, unique secrets (64+ chars for JWT_SECRET)
# 3. Rotate credentials regularly
# 4. Set NODE_ENV=production in production
# 5. Use environment variables in cloud platforms

